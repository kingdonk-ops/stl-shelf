# Version control / CI
.git
.github
.husky

# Dependencies & build output (rebuilt inside the image)
node_modules
dist
.output
.turbo
.wrangler
.alchemy

# Local env & secrets — never bake these into the image
.env
.env.*
!.env.example
.ngrok-url

# Editor / tooling
.vscode
.zed
.claude
.codex
.playwright-mcp
output

# Local data & large sample assets
data
assets/*.stl

# Docs & misc that aren't needed at build time
*.log
